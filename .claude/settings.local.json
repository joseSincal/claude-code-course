{
  "permissions": {
    "allow": [
      "Bash(docker-compose build *)",
      "Bash(docker-compose up *)",
      "Bash(docker-compose ps *)",
      "Bash(curl -s http://localhost:8000/courses)"
    ]
  }
}
